[build]
  # 构建命令
  command = "npm install && npm run build"
  # 构建输出目录（Vite 默认输出到 dist）
  publish = "dist"

# SPA 路由重定向配置（所有路由都指向 index.html）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 构建环境变量（如果需要）
[build.environment]
  NODE_VERSION = "18“

