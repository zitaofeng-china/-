/**
 * 路由配置
 * 占位路由组件，如需多页面功能可替换为 react-router
 */
import React from 'react'
import App from '../App'

// Placeholder router. Replace with react-router if multi-page is needed.
export function Router() {
  return <App />
}

export default Router

